# Faizan Khan — Portfolio

Personal portfolio site. Single-file build: Three.js (CDN) + vanilla JS + CSS, served as `index.html`.

**Live:** deployed on Netlify.

## Run locally
Just open `index.html` in a browser — no build step.
